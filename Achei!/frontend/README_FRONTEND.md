# Frontend do Achei!

Frontend simples em HTML, CSS e JavaScript puro.

Para rodar:

```cmd
cd frontend
py -m http.server 5500
```

Acesse:

```text
http://127.0.0.1:5500
```

Antes disso, mantenha o backend FastAPI rodando em `http://127.0.0.1:8000`.
